services:
  - type: web
    name: e-commerce-backend
    runtime: python
    buildCommand: pip install -r requirements.txt  # Use pip, no need to convert Poetry to requirements.txt
    startCommand: gunicorn core.wsgi:application
    envVars:
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: DATABASE_URL
        value: "postgresql://ecommerce_5l6z_user:iEJpgvc7xJ6awcZbm0YhMJl8pdRdUGoN@dpg-cuqvdvqj1k6c73cnafag-a.oregon-postgres.render.com/ecommerce_5l6z"  # Make sure to add your PostgreSQL URL
